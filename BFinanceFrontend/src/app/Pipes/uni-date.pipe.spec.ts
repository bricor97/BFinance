import { UniDatePipePipe } from './uni-date-pipe.pipe';

describe('UniDatePipePipe', () => {
  it('create an instance', () => {
    const pipe = new UniDatePipePipe();
    expect(pipe).toBeTruthy();
  });
});
