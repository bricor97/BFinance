import { SortByAccountPipe } from './sort-by-account.pipe';

describe('SortByAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new SortByAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
